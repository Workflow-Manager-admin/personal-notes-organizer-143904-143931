# personal-notes-organizer-143904-143931

## Notes Backend Database Setup

To initialize the SQLite database (with a test user and note), run:

```sh
cd notes_backend
node src/db-init.js
```

This creates `notes.sqlite` in the backend folder. Login with username `admin` and password `admin123`.